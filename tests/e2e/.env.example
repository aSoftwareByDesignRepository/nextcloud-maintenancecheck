# NC_E2E_* — any L2-allowed user (due board, customers).
# NC_ADMIN_* — NC admin or app admin (settings sections).
# Run scripts/e2e-prep.sh first on the local Docker stack to create/reset users.
NC_E2E_USER=mn_e2e
NC_E2E_PASS=
NC_ADMIN_USER=admin
NC_ADMIN_PASS=
