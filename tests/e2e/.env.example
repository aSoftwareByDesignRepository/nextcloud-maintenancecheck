# Copy to .env for local Playwright / axe runs (AC-19).
# NC_E2E_* — any L2-allowed user (due board, customers).
# NC_ADMIN_* — NC admin or app admin (settings sections).

NC_BASE_URL=http://localhost:8081
NC_E2E_USER=mn_e2e
NC_E2E_PASS=
NC_ADMIN_USER=admin
NC_ADMIN_PASS=
