# App Store screenshots

Nextcloud App Store allows **at most 10** screenshots (≤ 2 MiB each). This set uses **8**.

Browser chrome is cropped; images are 2× upscaled for sharper store display.

| File | Surface |
|------|---------|
| `maintenancecheck-screenshot-01.png` | Due board — overdue visits |
| `maintenancecheck-screenshot-02.png` | Visits — history and filters |
| `maintenancecheck-screenshot-03.png` | Customers register |
| `maintenancecheck-screenshot-04.png` | Catalogs — equipment types |
| `maintenancecheck-screenshot-05.png` | Work order detail |
| `maintenancecheck-screenshot-06.png` | Exceptions |
| `maintenancecheck-screenshot-07.png` | Ops KPI |
| `maintenancecheck-screenshot-08.png` | Dispatch — assign technician |

Keep `appinfo/info.xml` `<screenshot>` URLs on the default branch (`main`) in sync after pushing new PNGs.
